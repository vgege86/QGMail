version: "3.9"

services:
  qgmail:
    image: ghcr.io/vgege86/qgmail:latest
    ports:
      - "3000:3000"
    env_file: .env
    volumes:
      - ./sessions:/app/sessions
    restart: unless-stopped
